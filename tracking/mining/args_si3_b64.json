{
  "alpha_expressions": [
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 5), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 3), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 7), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 15), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 5), 60)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 5), 180)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(mean_loan_rate))), 5), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, group_rank(vec_avg(shrt3_bar), sector)), 5), 90)"
  ],
  "instrument_type": "EQUITY",
  "region": "USA",
  "universe": "TOP3000",
  "delay": 1,
  "decay": 6,
  "neutralization": "STATISTICAL",
  "truncation": 0.08,
  "test_period": "P0Y0M",
  "unit_handling": "VERIFY",
  "nan_handling": "ON",
  "language": "FASTEXPR",
  "visualization": false,
  "pasteurization": "ON",
  "max_trade": "ON"
}
