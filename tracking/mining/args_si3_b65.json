{
  "alpha_expressions": [
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 20), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 30), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 40), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 61), 90)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 15), 60)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 15), 180)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 15), 250)",
    "ts_decay_linear(signed_power(subtract(0.5, rank(vec_avg(shrt3_bar))), 25), 120)"
  ],
  "instrument_type": "EQUITY",
  "region": "USA",
  "universe": "TOP3000",
  "delay": 1,
  "decay": 6,
  "neutralization": "STATISTICAL",
  "truncation": 0.08,
  "test_period": "P0Y0M",
  "unit_handling": "VERIFY",
  "nan_handling": "ON",
  "language": "FASTEXPR",
  "visualization": false,
  "pasteurization": "ON",
  "max_trade": "ON"
}
