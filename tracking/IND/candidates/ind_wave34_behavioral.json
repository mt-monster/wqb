[
  "-rank(vec_avg(price_path_curvature_measure))",
  "-rank(vec_avg(visual_price_path_shape_score))",
  "-rank(ts_zscore(vec_avg(salience_weighted_return_score),63))",
  "-rank(vec_avg(consecutive_return_streak_length))",
  "rank(vec_avg(chronological_return_sequence_correlation))"
]
