[
  "rank(vec_avg(global_percentile_acquisition_likelihood))",
  "rank(vec_avg(country_percentile_acquisition_likelihood))",
  "rank(vec_avg(industry_percentile_acquisition_likelihood))",
  "rank(vec_avg(sector_percentile_acquisition_likelihood))",
  "rank(vec_avg(region_percentile_acquisition_likelihood))",
  "-rank(vec_avg(region_valuation_percentile_rank))",
  "-rank(vec_avg(region_credit_quality_percentile_rank))",
  "rank(ts_mean(vec_avg(global_percentile_acquisition_likelihood), 21))"
]
