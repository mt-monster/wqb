{
  "batch": "b96",
  "dataset": "analyst7 - QP98oPgr prod_corr optimization",
  "hypothesis": "QP98oPgr (convex5 P5 D10 @SUBINDUSTRY) passes all RA checks (sh1.58/fit1.13/tvr5.37%/2Y2.58) but prod_corr=0.8111 >> 0.7. est_q_ner_raised_1wk is a popular field with many production alphas. Strategy: change neutralization to SLOW_AND_FAST (different risk model = different prod_corr), use ts_rank instead of group_rank (time-series normalization vs cross-sectional), try composite signals via subtract (ner-sal revision differential), and vary power/decay. The convex5 template that boosted Sharpe should still work with these changes.",
  "alpha_expressions": [
    "ts_decay_linear(signed_power(subtract(group_rank(est_q_ner_raised_1wk, subindustry), 0.5), 5), 10)",
    "ts_decay_linear(signed_power(subtract(ts_rank(est_q_ner_raised_1wk, 60), 0.5), 5), 10)",
    "ts_decay_linear(signed_power(subtract(group_rank(est_q_ner_raised_1wk, sector), 0.5), 5), 10)",
    "ts_decay_linear(signed_power(subtract(group_rank(est_q_ner_raised_1wk, industry), 0.5), 5), 10)",
    "ts_decay_linear(signed_power(subtract(group_rank(est_q_ner_raised_1wk, subindustry), 0.5), 5), 20)",
    "ts_decay_linear(signed_power(subtract(group_rank(est_q_ner_raised_1wk, subindustry), 0.5), 3), 10)",
    "ts_decay_linear(signed_power(subtract(subtract(group_rank(est_q_ner_raised_1wk, subindustry), group_rank(est_q_sal_raised_1wk, subindustry)), 0.5), 5), 10)",
    "ts_decay_linear(signed_power(subtract(rank(est_q_ner_raised_1wk), 0.5), 5), 10)"
  ],
  "instrument_type": "EQUITY",
  "region": "USA",
  "universe": "TOP3000",
  "delay": 1,
  "decay": 6,
  "neutralization": "SLOW_AND_FAST",
  "truncation": 0.08,
  "test_period": "P0Y0M",
  "unit_handling": "VERIFY",
  "nan_handling": "ON",
  "language": "FASTEXPR",
  "visualization": false,
  "pasteurization": "ON",
  "max_trade": "ON",
  "style_notes": {
    "expr1": "original QP98oPgr expr @ SLOW_AND_FAST instead of SUBINDUSTRY",
    "expr2": "ts_rank w60 instead of group_rank - time-series normalization",
    "expr3": "sector group instead of subindustry",
    "expr4": "industry group instead of subindustry",
    "expr5": "decay20 instead of 10 - slightly longer for less turnover sensitivity",
    "expr6": "power3 instead of 5 - gentler convexity",
    "expr7": "composite: ner rank - sal rank (revision differential) + convex5",
    "expr8": "plain rank instead of group_rank - simpler cross-sectional"
  }
}
