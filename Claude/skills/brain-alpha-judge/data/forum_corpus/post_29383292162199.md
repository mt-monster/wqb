---
title: "【课代表】最新genius排名模拟代码"
author: "XX42289"
source_url: "https://support.worldquantbrain.com/hc/zh-cn/community/posts/29383292162199-课代表-最新genius排名模拟代码"
source_post_id: "29383292162199"
source_query: "XX42289"
post_date: "2025-01-21T09:10:19Z"
post_votes: 34
total_comments: 3
selected_comments: 1
crawled_at: "2026-04-12T17:50:48.3821558+08:00"
admission_reason: >-
  Same author handle as visible search-result post 28856000657303, which states the
  author reached a 0.99 value factor and roughly 90 USD/day base pay.
---

# Original Post Body

~~~text
只需要修改以下参数：# 参数设置user_id = 'ZZ13271' # 你的ID，JL71699，XX42289, TL87739，HZ69256mode = 'expert' # 'expert', 'master', 'grandmaster'draw_plots = False # 是否画图print_info = True # 是否打印信息quarter_pay_num = 40 # 假设满足季度奖励的人需要交满40个combine = True # 是否考虑combinedAlphaPerformance和combinedSelectedAlphaPerformanceseason = "2025-01-01" # 每个季度的第一天read_old = False # 是否读取已有的数据# 1. 遍历分页 API，收集所有用户数据import datetimeimport os.pathimport jsonimport timeimport numpy as npimport requestsimport pandas as pdfrom user_package.machine_lib import loginpd.set_option('expand_frame_repr', False)pd.set_option('display.max_rows', 50000)def fetch_all_data(s, season):offset = 0limit = 30all_data = []while True:response = s.get(f"{base_url}?limit={limit}&offset={offset}&date={season}&order%3D-fieldAvg&aggregate=user", )data = response.json()if "results" in data:all_data.extend(data["results"])offset += limitprint(f"Fetched {offset} records")if offset >= data["count"]:breakelse:print("Error fetching data")breaktime.sleep(0)# 保存为CSV文件df = pd.DataFrame(all_data)os.makedirs('genius', exist_ok=True)df.to_csv(f"genius/all_users_data_season{season}_{datetime.datetime.now()}.csv", index=False)df.to_csv(f"all_users_data.csv", index=False)print(f"Data saved to genius/all_users_data_season{season}_{datetime.datetime.now()}.csv")return dfdef rank(df, self_data, cols):# 计算自己的排名self_rank = {}tt_rk = 0for col in cols:if col in ["operatorCount", "fieldCount", "communityActivity", "completedReferrals", "maxSimulationStreak"]:self_val = self_data.get(col)rank = (df[col] > self_val).sum() + 1self_rank[col] = str(int(rank))elif col in ["operatorAvg", "fieldAvg"]:self_val = self_data.get(col)rank = (df[col] < self_val).sum() + 1self_rank[col] = str(int(rank))tt_rk += rankself_rank["total_rank"] = tt_rkreturn self_rankdef calculate_ranks(df, cols, weights=[1, 1, 1, 1, 1, 1, 1]):# 创建一个新的DataFrame来存储排名rank_df = pd.DataFrame(index=df.index, columns=cols)# 遍历每一列for col in cols:# 计算排名，对于数值越大越好的列，使用df[col].rank(method='min', ascending=False)# 对于数值越小越好的列，使用df[col].rank(method='min', ascending=True)if col in ["operatorCount", "fieldCount", "communityActivity","completedReferrals", "maxSimulationStreak"]:rank_df[f"{col}_rank"] = df[col].rank(method='min', ascending=False)elif col in ["operatorAvg", "fieldAvg"]:rank_df[f"{col}_rank"] = df[col].rank(method='min', ascending=True)del rank_df[col](operatorCount_weight, operatorAvg_weight,fieldCount_weight, fieldAvg_weight,communityActivity_weight, completedReferrals_weight,maxSimulationStreak_weight) = weightsrank_df[f"operatorCount_rank"] = rank_df[f"operatorCount_rank"] * operatorCount_weightrank_df[f"operatorAvg_rank"] = rank_df[f"operatorAvg_rank"] * operatorAvg_weightrank_df[f"fieldCount_rank"] = rank_df[f"fieldCount_rank"] * fieldCount_weightrank_df[f"fieldAvg_rank"] = rank_df[f"fieldAvg_rank"] * fieldAvg_weightrank_df[f"communityActivity_rank"] = rank_df[f"communityActivity_rank"] * communityActivity_weightrank_df[f"completedReferrals_rank"] = rank_df[f"completedReferrals_rank"] * completedReferrals_weightrank_df[f"maxSimulationStreak_rank"] = rank_df[f"maxSimulationStreak_rank"] * maxSimulationStreak_weight# for col in cols:#     rank_df[f"{col}_rank"] = (rank_df[f"{col}_rank"] - 1) / rank_df[f"{col}_rank"].max()# rank_df[f"{col}_rank"] = (rank_df[f"{col}_rank"] - 1) / len(rank_df)# if col in ['communityActivity', 'completedReferrals']:#     rank_df[f"{col}_rank"] = rank_df[f"{col}_rank"] / 2# del rank_df['maxSimulationStreak_rank']rank_df['total_rank'] = rank_df.sum(axis=1)return rank_dfif __name__ == '__main__':# 参数设置user_id = 'ZZ13271'  # 你的ID，JL71699，XX42289, TL87739，HZ69256mode = 'expert'  # 'expert', 'master', 'grandmaster'draw_plots = False  # 是否画图print_info = True  # 是否打印信息quarter_pay_num = 40  # 假设满足季度奖励的人需要交满40个combine = True  # 是否考虑combinedAlphaPerformance和combinedSelectedAlphaPerformanceseason = "2025-01-01"  # 每个季度的第一天read_old = False  # 是否读取已有的数据s = login()# API的基本信息base_url = "https://api.worldquantbrain.com/consultant/boards/genius"# 执行步骤if read_old:df = pd.read_csv("all_users_data.csv")else:df = fetch_all_data(s, season)df.reset_index(drop=True, inplace=True)self_data = df.loc[df['user']==user_id].to_dict(orient='records')[0]# self_data['operatorCount'] = 150# self_data['operatorAvg'] = 6.5# self_data['fieldAvg'] = 3.0# self_data['fieldCount'] = 350# self_data['communityActivity'] = 65# self_data['maxSimulationStreak'] = 146# self_data['completedReferrals'] = 1print(self_data)# 再计算一下自己每个标签的百分比排名my_rank = rank(df, self_data,["operatorCount", "operatorAvg", "fieldCount", "fieldAvg", "communityActivity", "completedReferrals","maxSimulationStreak"])if print_info:print("|+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+|")print("|+=+==+=+=以下是按照gold为universe计算的排名=+==+=+==+=+|")print("|+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+|")print(user_id)print("GOLD总人数:", len(df))print(f"可能满足季度奖金的总人数（交满{quarter_pay_num}个）:", len(df[df['alphaCount']>=quarter_pay_num]))print("以下是你各项的排名:")for k, v in my_rank.items():print(f"{k}: {v}, 越小越好")cols = ["operatorCount", "operatorAvg","fieldCount", "fieldAvg","communityActivity", "completedReferrals","maxSimulationStreak"]rank_df = calculate_ranks(df.set_index('user'), cols)rank_df = pd.merge(rank_df, df[['user', 'alphaCount', 'pyramidCount', 'combinedAlphaPerformance', 'combinedSelectedAlphaPerformance', "operatorCount", "operatorAvg", "fieldCount", "fieldAvg", "communityActivity", "completedReferrals", 'maxSimulationStreak']],on='user', how='left')rank_df['预测排名（不准，以官方为准）'] = rank_df['total_rank'].rank(method='min', ascending=True).apply(lambda x: int(x))rank_df[['user', '预测排名（不准，以官方为准）', 'alphaCount', 'pyramidCount', "operatorAvg", "fieldAvg"]].to_csv('temp/rank预计排名（非官方，仅供参考）.csv', index=False, encoding='gbk')if mode == 'expert':rank_df = rank_df[rank_df['alphaCount'] >= 20]rank_df = rank_df[rank_df['pyramidCount'] >= 10]if combine:rank_df = rank_df[(rank_df['combinedAlphaPerformance'] >= 0.5) | (rank_df['combinedSelectedAlphaPerformance'] >= 0.5)]elif mode == 'master':rank_df = rank_df[rank_df['alphaCount'] >= 120]rank_df = rank_df[rank_df['pyramidCount'] >= 30]if combine:rank_df = rank_df[(rank_df['combinedAlphaPerformance'] >= 1) | (rank_df['combinedSelectedAlphaPerformance'] >= 1)]elif mode == 'grandmaster':rank_df = rank_df[rank_df['alphaCount'] >= 220]rank_df = rank_df[rank_df['pyramidCount'] >= 60]if combine:rank_df = rank_df[(rank_df['combinedAlphaPerformance'] >= 2) | (rank_df['combinedSelectedAlphaPerformance'] >= 2)]rank_df[['user', '预测排名（不准，以官方为准）', 'alphaCount', 'pyramidCount', "operatorCount", "operatorAvg", "fieldCount", "fieldAvg", "communityActivity", "completedReferrals","maxSimulationStreak"]].to_csv('temp/rank预计排名（非官方，仅供参考）.csv', index=False, encoding='gbk')my_total_rank = (rank_df['total_rank'] < my_rank['total_rank']).sum()if print_info:print("我的排名:", my_total_rank)print(f"满足{mode}条件的总人数:", len(rank_df))if mode == 'grandmaster':print(rank_str:=f'你想成为【{mode}】', "至少要排名在:", threshold := int(len(df[df['alphaCount']>=quarter_pay_num]) * 0.02))elif mode == 'master':print(rank_str:=f'你想成为【{mode}】', "至少要排名在:", threshold := int(len(df[df['alphaCount']>=quarter_pay_num]) * 0.08))elif mode == 'expert':print(rank_str:=f'你想成为【{mode}】', "至少要排名在:", threshold := int(len(df[df['alphaCount']>=quarter_pay_num]) * 0.20))my_alphaCount = self_data.get('alphaCount')my_pyramidCount = self_data.get('pyramidCount')my_combinedAlphaPerformance = self_data.get('combinedAlphaPerformance')my_combinedSelectedAlphaPerformance = self_data.get('combinedSelectedAlphaPerformance')alphaCount_tag, pyramidCount_tag, combinedAlphaPerformance_tag, total_rank_tag = False, False, False, Falseif mode == 'grandmaster':if my_alphaCount >= 220:alphaCount_tag = Trueif my_pyramidCount >= 60:pyramidCount_tag = Trueif my_combinedAlphaPerformance >= 2 or my_combinedSelectedAlphaPerformance >= 2:combinedAlphaPerformance_tag = Trueif my_total_rank < threshold:total_rank_tag = Trueelif mode == 'master':if my_alphaCount >= 120:alphaCount_tag = Trueif my_pyramidCount >= 30:pyramidCount_tag = Trueif my_combinedAlphaPerformance >= 1 or my_combinedSelectedAlphaPerformance >= 1:combinedAlphaPerformance_tag = Trueif my_total_rank < threshold:total_rank_tag = Trueelif mode == 'expert':if my_alphaCount >= 20:alphaCount_tag = Trueif my_pyramidCount >= 10:pyramidCount_tag = Trueif my_combinedAlphaPerformance >= 0.5 or my_combinedSelectedAlphaPerformance >= 0.5:combinedAlphaPerformance_tag = Trueif my_total_rank < threshold:total_rank_tag = Trueif alphaCount_tag and pyramidCount_tag and combinedAlphaPerformance_tag and total_rank_tag:print(f"恭喜你大概率可以成为【{mode}】")else:if not alphaCount_tag:print(f"你的alphaCount为{my_alphaCount}, 未达到{mode}标准")if not pyramidCount_tag:print(f"你的pyramidCount为{my_pyramidCount}, 未达到{mode}标准")if not combinedAlphaPerformance_tag:print(f"你的combinedAlphaPerformance为{max(my_combinedAlphaPerformance, my_combinedSelectedAlphaPerformance)}, 未达到{mode}标准")if not total_rank_tag:print(f"你的总排名为{my_total_rank}, 未达到{mode}标准")print("同志还需努力！！！")rank_df = calculate_ranks(rank_df.set_index('user'), cols)rank_df = pd.merge(rank_df, df[['user', 'alphaCount', 'pyramidCount', 'combinedAlphaPerformance', 'combinedSelectedAlphaPerformance',"operatorCount", "operatorAvg", "fieldCount", "fieldAvg", "communityActivity", "completedReferrals",'maxSimulationStreak']],on='user', how='left')# 再计算一下自己每个标签的百分比排名my_rank = rank(rank_df, self_data,["operatorCount", "operatorAvg", "fieldCount", "fieldAvg", "communityActivity", "completedReferrals","maxSimulationStreak"])rank_df['预测排名（不准，以官方为准）'] = rank_df['total_rank'].rank(method='min', ascending=True).apply(lambda x: int(x))rank_df[['user', '预测排名（不准，以官方为准）', 'alphaCount', 'pyramidCount', "operatorCount", "operatorAvg", "fieldCount", "fieldAvg", "communityActivity", "completedReferrals","maxSimulationStreak"]].to_csv(f'temp/rank相对等级{mode}.csv', index=False, encoding='gbk')my_total_rank = (rank_df['total_rank'] < my_rank['total_rank']).sum()if print_info:print("|+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+|")print(f"|+=+==+=+=以下是按照{mode}为universe计算的排名=+==+=+==+=+|")print("|+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+=+==+|")print(user_id)print(f"满足{mode}总人数:", len(rank_df))print(f"以下是你在{mode}池子里的各项的排名:")for k, v in my_rank.items():print(f"{k}: {v}, 越小越好")print(f"以{mode}作为池子的总排名：", my_total_rank)
~~~

# Selected Comments

## XX42289 on 1年前

~~~text
ZZ13271感谢老虎哥的报告。程序在Linux上是正常的，但是在Windows上会出现问题。请你找到任何关于的代码df.to_csv(f"genius/all_users_data_season{season}_{datetime.datetime.now()}.csv", index=False)其中有一个datetime.datetime.now()，这个里面会自动生成 系统当前的时间，如：2025-01-22 17:50:38.10808但是Windows不支持文件名有:，所以你可以注释掉这两行代码。也可以改成str(datetime.datetime.now()).replace(':','_')TL42173
~~~