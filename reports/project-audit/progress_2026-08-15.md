# 因子挖掘进展落地报告（2026-08-15）

> 落地前次建议「1. 刷新 KOR campaign_state」「2. 统一提交状态、算准总提交数」。
> 本次通过**直连 WorldQuant BRAIN 平台核验**完成，发现重大数据同步问题。

## 一、交付物

| 文件 | 说明 |
|------|------|
| `tracking/progress_ledger.json` | 统一进度台账：本地 257 个 alpha 与平台 OS 真相交叉核对 |
| `tracking/KOR/results/kor_d1_campaign_state.json`（已备份 `.bak`） | 刷新后的战役状态，对齐 wave 前沿 + 平台对账 |
| `tracking/_os_ids.json` | 平台 OS 全集 146 个 alpha id 快照（平台真相源） |
| `tools/build_progress_ledger.py` | 可复跑的台账构建脚本（依赖 `_os_ids.json`） |

## 二、关键结论（颠覆性）

**本地追踪的 alpha 全部未提交至平台。**

- 平台 OS 全量 **146** 个（已完整拉取）：`USA 133 / IND 4 / MEA 3 / GBR 3 / GLB 1 / KOR 1 / ASI 1`。
- 本地 `tracking/` 共追踪 **257** 个 alpha id，与平台 OS 集合**交集为 0**。
- 抽样核验：EUR `0mpjQ85r` 在平台为 `stage=IS, status=UNSUBMITTED`（确为真实 8 位 alpha id，但仅处于样本内）。

**本地"已提交"标记不可信：**

| 区域 | 本地记录 | 平台核验状态 | 说明 |
|------|---------:|--------------|------|
| EUR | 117 | UNSUBMITTED ×117 | 均为 8 位有效 id，平台处于 IS |
| USA | 58 | UNSUBMITTED ×58 | 同上 |
| IND | 6 | UNSUBMITTED ×6 | 同上 |
| KOR | 66 | INVALID_TRACKING_ID ×66 | campaign_state 的 `id` 为 21–24 位，**不是标准 8 位 alpha id**，无法在平台核验；其 `submitted=39` 为本地臆测 |
| GLB | 10 | SUBMITTED_UNVERIFIED ×10 | batch 文件 `alpha_id` 为空，仅 `status=SUBMITTED` 文本，无 id 可核验 |

**经平台核验已提交数 = 0**（针对 `tracking/` 收录的 alpha）。

## 三、campaign_state 刷新内容

- `frontier_wave = 34`、`total_wave_files = 102`、`total_candidate_exprs = 741`
- `wave_inventory`：逐 wave 的 file/exprs/settings/datasets 清单（共 23 个 wave 编号，102 个文件）
- `cache_metrics`：KOR/cache/metrics 下 6 个回测结果缓存，**0 通过 / 6 失败**（最新前沿 wave 活动产物全部未过检查）
- `platform_reconciliation` + `reconciliation_note`：明确标注 campaign_state 与平台不符，提交状态以平台 OS 为准

> 注：原 campaign_state 仅记录至 wave 16，已落后于实际 wave 文件前沿（34）。本次**未伪造** 17–34 的回测结果——这些 wave 文件本地只有候选表达式，无回测结果，已如实标注为候选。

## 四、根因与下一步

**根因**：`tracking/` 与平台状态严重不同步。平台上真实提交活动（146 个 OS，其中 USA 133）**未被本地追踪文件收录**；而本地追踪的 257 个 alpha 多为历史 IS 模拟产物，从未提交。

建议：
1. **以平台为准重建进度账本**：把平台 OS 的 146 个 alpha（尤其 USA 133）按 id 回填进 `tracking/`，替换当前基于本地文件的统计。
2. **修正 KOR/GLB 提交标记**：KOR 的 66 个 id 无效（非 alpha id），GLB 缺 id——需从实际提交记录补全真实 alpha id。
3. **收敛 KOR 前沿质量**：cache/metrics 最新 6 个 alpha 全部 failed，建议先定位 failed_checks 共性再继续扩 wave。
4. 复跑 `tools/build_progress_ledger.py` 前，先用 `get_user_alphas(stage=OS)` 刷新 `tracking/_os_ids.json` 即可重算。
