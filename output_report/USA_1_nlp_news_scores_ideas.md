# Feature Engineering Ideas: nlp_news_scores (USA, D1)

**Dataset**: nlp_news_scores
**Region**: USA
**Delay**: 1

## 字段分类

本数据集包含以下字段类别：
- 情感极性字段：sentiment_score, positive_score, negative_score
- 情感强度字段：sentiment_magnitude, sentiment_confidence
- 文本特征字段：word_count, sentence_count, readability
- 新闻分类字段：news_category, topic_score

## 特征工程建议

基于成功案例 USA-MFP-4LEG-WIN 的骨架模式，生成以下特征概念：

---

**Concept**: Multi-leg sentiment combination captures diverse narrative signals
- **Implementation Example**: `rank(add(multiply(0.4, rank(ts_backfill({sentiment_score}, 66))), multiply(0.3, rank(ts_backfill({positive_score}, 66))), multiply(0.3, rank(ts_backfill({negative_score}, 66)))))`

---

**Concept**: Sentiment momentum with backfill preprocessing captures trend continuation
- **Implementation Example**: `rank(ts_delta(ts_backfill({sentiment_score}, 66), 5))`

---

**Concept**: Sentiment-price divergence with backfill captures contrarian opportunities
- **Implementation Example**: `rank(ts_corr(ts_backfill({sentiment_score}, 66), ts_delta(close, 1), 20))`

---

**Concept**: Weighted sentiment combination captures balanced narrative
- **Implementation Example**: `rank(add(multiply(0.5, rank(ts_backfill({sentiment_score}, 66))), multiply(0.5, rank(ts_backfill({sentiment_magnitude}, 66)))))`

---

**Concept**: Sentiment persistence with backfill captures sustained narrative
- **Implementation Example**: `rank(ts_mean(ts_backfill({sentiment_score}, 66), 10))`

---

**Concept**: Positive-negative sentiment imbalance with backfill captures directional bias
- **Implementation Example**: `rank(ts_backfill(({positive_score} - {negative_score}) / ({positive_score} + {negative_score} + 0.001), 66))`

---

**Concept**: Sentiment volatility with backfill captures uncertainty
- **Implementation Example**: `rank(ts_std(ts_backfill({sentiment_score}, 66), 20))`

---

**Concept**: Two-leg sentiment-magnitude combination captures conviction-weighted narrative
- **Implementation Example**: `rank(add(multiply(0.6, rank(ts_backfill({sentiment_score}, 66))), multiply(0.4, rank(ts_backfill({sentiment_magnitude}, 66)))))`

---

*Generated by manual feature engineering analysis based on USA-MFP-4LEG-WIN pattern*
*Dataset: nlp_news_scores, Region: USA, Delay: 1, Universe: TOP3000*
