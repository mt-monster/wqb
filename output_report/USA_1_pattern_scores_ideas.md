# Feature Engineering Ideas: pattern_scores (USA, D1)

**Dataset**: pattern_scores
**Region**: USA
**Delay**: 1

## 字段分类

本数据集包含 504 个字段，主要包括以下类别：
- 模式识别字段：各种技术形态评分
- 趋势指标字段：趋势强度、方向等
- 波动率字段：历史波动率、隐含波动率等
- 成交量字段：成交量模式、换手率等

## 特征工程建议

基于成功案例 USA-MFP-4LEG-WIN 的骨架模式，生成以下特征概念：

---

**Concept**: Multi-leg pattern combination captures diverse technical signals
- **Implementation Example**: `rank(add(multiply(0.4, rank(ts_backfill({pattern_field_1}, 66))), multiply(0.3, rank(ts_backfill({pattern_field_2}, 66))), multiply(0.3, rank(ts_backfill({pattern_field_3}, 66)))))`

---

**Concept**: Pattern momentum with backfill preprocessing captures trend continuation
- **Implementation Example**: `rank(ts_delta(ts_backfill({pattern_field}, 66), 5))`

---

**Concept**: Pattern-price divergence with backfill captures contrarian opportunities
- **Implementation Example**: `rank(ts_corr(ts_backfill({pattern_field}, 66), ts_delta(close, 1), 20))`

---

**Concept**: Weighted pattern combination captures balanced technical signals
- **Implementation Example**: `rank(add(multiply(0.5, rank(ts_backfill({pattern_field_1}, 66))), multiply(0.5, rank(ts_backfill({pattern_field_2}, 66)))))`

---

**Concept**: Pattern persistence with backfill captures sustained technical signals
- **Implementation Example**: `rank(ts_mean(ts_backfill({pattern_field}, 66), 10))`

---

**Concept**: Pattern volatility with backfill captures uncertainty
- **Implementation Example**: `rank(ts_std(ts_backfill({pattern_field}, 66), 20))`

---

**Concept**: Two-leg pattern combination captures conviction-weighted signals
- **Implementation Example**: `rank(add(multiply(0.6, rank(ts_backfill({pattern_field_1}, 66))), multiply(0.4, rank(ts_backfill({pattern_field_2}, 66)))))`

---

**Concept**: Pattern reversal captures overreaction correction
- **Implementation Example**: `-rank(ts_mean(ts_backfill({pattern_field}, 66), 20))`

---

*Generated by manual feature engineering analysis based on USA-MFP-4LEG-WIN pattern*
*Dataset: pattern_scores, Region: USA, Delay: 1, Universe: TOP3000*
